export enum MouseEventType {
  DOWN = 'mousedown',
  MOVE = 'mousemove',
  UP = 'mouseup',
  LEAVE = 'mouseleave',
}

export enum ShapeType {
  RECTANGLE = 'rectangle',
  CIRCLE = 'circle',
  PENCIL = 'pencil',
}
